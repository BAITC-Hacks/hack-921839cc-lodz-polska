# Проверка интеграции 23 сентября 2026

## Общая сборка frontend + backend + AI

Для объединения взяты backend `d0941faa18a3247f71b466a179c97b647c97d019` и frontend `ac2b05fbe8d9bad0f994a84170ccb3542563007e`. Вторая версия содержит свежее исправление таймаута советника; оно сохранено. AI/report уже включены в backend и сопоставлены с актуальным `feature/ai @ 45c9169`: содержимое всех 13 файлов совпадает после нормализации переводов строк. История AI-ветки с ранее обнаруженным секретом не добавляется; причины описаны в security-review.md.

Проверено на Windows, Python 3.12.14, закреплённые зависимости из requirements-dev.txt:

- 93 backend-теста без пропусков, backend-owned line coverage 99% (AI/report не входят в метрику).
- 49 frontend-тестов, TypeScript, ESLint, production-сборка.
- `python backend/scripts/check_ai_contract.py`: 25 тестов; это часть общего набора, не дополнительные 25.
- Двусторонняя проверка реального frontend-запроса и FastAPI-ответа: Node → временный request.json → Python TestClient → ai-response.json → Zod/normalizeAnalysis.
- Ruff check и format --check; повторная генерация семи fixtures и OpenAPI не меняет их содержимое.
- Production Next.js в браузере → настоящий FastAPI → неизменённый simulation engine: каталог, пример 95/56.54307, финализация, AI 503 и ручной повтор, поиск M5/Сарыарка → M3/Нура, разбор предложенного плана, сохранение исходного, применение, новый итог 100/57.20556 и сравнение двух сохранённых планов.
- Через HTTP проверены советник (200 для допустимого добавления и 502 AI_INVALID_ADVICE для конфликта M5/M13 в одном районе) и отчёт (Score 56.54307, расход 95, Markdown).

AI в этом прогоне — явно контролируемый тестовый провайдер. Внешняя модель не вызывалась: локальный ключ не настроен. Контрольный провайдер и сервер проверки находятся только в рабочей папке, не в репозитории и не в production-коде. Docker недоступен, сборка образа не проверялась.

Решение по советнику: серверный маршрут сохраняется как доступная интеграция для 0–4 решений и проверки через Swagger; для интерфейсного демо достаточно анализа полного плана и одной проверенной замены. Отдельная панель советника в UI не добавлялась. Совместного AI-сравнения двух планов пока нет; флаг `NEXT_PUBLIC_ENABLE_AI_COMPARISON=false` сохраняется.

Workflow теперь проверяет обе части и их границу. Последний известный до публикации общий CI запуск `35859799210` завершился failure до выполнения шагов; доступного лога нет. Его нельзя выдавать за успешную CI-проверку. Статус нового запуска проверяется после публикации merge-коммита.

## Предыдущие проверки (историческая запись)

## Проверенные версии

- Frontend: `2412b1e16b10c892fa66b5eaeaf040c6795907e2`.
- AI: исходники `49ab13dd3407f47812745327a5c01431f1ce2fa6`; в последующем `f008e12c30fabe0ee85295aa8a4f4cc536149162` изменён только `.env.example`, исходники остались теми же.
- Общая база main: `573ee0b729a64a919364f5c006321641937afae9`.

Файлы frontend и AI были загружены для независимой проверки и не включены в backend-коммит. Работа backend идёт в `feature/simulation`, PR #3. Публичный адаптер учёл обновление `frontend/lib/ai-contract.ts`, поэтому участникам не требуется менять их код ради начального формата `{simulation: ...}`.

## Результаты

- 78 автоматических тестов прошли с подключённым реальным AI-модулем и контролируемым провайдером. Покрытие строк принадлежащего backend кода: 99%; расчётный engine и validator: 100%. Процент покрытия не является доказательством отсутствия всех возможных ошибок.
- Ruff check и format --check прошли для всех backend-owned Python-файлов.
- Шесть типов JSON-ответов engine прошли реальные Zod-схемы фронтенда: districts, measures, preview, invalid finalize, official result, recommendation.
- Настоящая функция frontend `createAnalysisRequest()` сформировала запрос; backend принял его, ответ прошёл настоящий `aiResponseSchema` и `normalizeAnalysis()`.
- Через настоящий `frontend/lib/api.ts` выполнены HTTP-вызовы к запущенному Uvicorn: загрузка 5 районов/14 мер, пустой preview, отклонение M1+M3, finalize официального примера, поиск замены и обработка AI 503. Полный набор занял около 0.36 секунды в данной среде; это smoke-измерение, не нагрузочный benchmark.
- Точные контроли: 52.55768 → 56.54307; бюджет 95. Найденный лучший сосед с одной заменой: 57.20556. Все его ограничения повторно проверены.
- Проверена защита AI-границы: поддельные score, budget, names, costs, indicators клиента не попадают в AI вместо серверных фактов.
- Испытан настоящий report builder AI-участника; он получает проверенные числа и возвращает ExecutiveBrief.
- GitHub подтвердил отсутствие конфликта с main для первого backend-коммита. Модули остальных участников не изменялись. Перед merge следует повторно проверить актуальную mergeability.

## Исправление таймаутов и повторная реальная проверка

Лимиты согласованы: OpenAI SDK — 45 секунд без автоматических повторов, frontend AI-маршруты — 60 секунд, расчётные маршруты — прежние 20 секунд. Исправлено ошибочное сообщение «неверный JSON», когда браузер прерывает чтение тела ответа по таймауту. В SDK это сетевой таймаут, а не абсолютная гарантия длительности запроса. Обе правки находятся в своих feature-ветках: backend PR #3, frontend PR #2; при объединении нужны обе.

После изменения выполнены **5 последовательных реальных запросов** через настоящий frontend `api.analyze()` → Uvicorn/FastAPI → OpenAI `gpt-6-astra` → frontend Zod и нормализатор. Диагностического увеличения таймаута или подставного AI-ответа в этом прогоне нет. Проверены три сценария: официальный, предложенная движком замена и перенос M10 в Сарыарку. Все пять ответов прошли; длительности — **18.95, 18.74, 17.82, 17.98, 17.00 секунды**. Каждый превысил прежний лимит backend 15 секунд. Числовые результаты остались неизменными. Во время генерации отдельный расчёт базового сценария отвечал за **28–46 мс**. Это небольшой функциональный прогон, не нагрузочный тест и не гарантия задержки внешнего API.

Frontend: **26 тестов прошли**, включая ответ AI через 25 секунд, прерывание зависшего запроса через 60 секунд, чтение JSON при таймауте, сохранение сообщения HTTP 503, отсутствие автоматического повторного запроса и очистку таймеров. Backend: реальный SDK с `MockTransport` подтвердил таймаут 45 секунд, ровно одну попытку, безопасный HTTP 503 и работоспособность расчёта после сбоя провайдера. Ключи не включены в опубликованные файлы.

## Первоначальная диагностика и границы проверки

Полный рендер Next.js в этой среде не выполнен: запуск блокируется Windows-ошибкой `spawn EPERM` при создании дочернего процесса. Проверен именно настоящий API-клиент фронтенда по HTTP, а также его runtime-схемы. Визуальную проверку выполняет владелец frontend.

После предоставления ключа выполнена отдельная реальная проверка OpenAI API: доступ к `gpt-6-astra` подтверждён, вызов `/api/ai/analyze` через FastAPI TestClient и настоящий AI-провайдер вернул HTTP 200 за 13.4 секунды. Ответ прошёл Pydantic-модель backend, настоящий `aiResponseSchema` и `normalizeAnalysis()` frontend. Контрольные числа в тексте совпали с серверным расчётом. Ключ в этот коммит не включён.

До исправления первый запрос завершился `APITimeoutError` на старом лимите 15 секунд; следующий диагностический запрос использовал лимит 60 секунд и завершился за 13.4 секунды. Затем лимиты были исправлены и выполнен отдельный прогон из пяти запросов, описанный выше. Мок-тесты по-прежнему используют контролируемый провайдер.

В AI-модуле есть предупреждения об устаревших Pydantic `.dict()`/`.parse_obj()`; на закреплённой Pydantic 2.13.5 они работают. AI-код не редактировался; при миграции на Pydantic 3 его владельцу потребуется заменить эти вызовы.

Docker здесь не установлен, поэтому образ не собирался. В репозитории есть Dockerfile, compose и CI-проверки; локальный Python backend и его HTTP-запуск проверены.

## Перед объединением

Объединять через PR с сохранением текущего main. Feature-ветки frontend и AI добавляют свои директории; backend-папки и shared принадлежат интегратору. Не подключать AI `create_ai_router()` поверх интеграционного адаптера: один и тот же путь появится дважды и обойдёт серверный пересчёт. Сохранить текущие `app.main` и общий контракт.

После объединения: установить backend requirements-dev/requirements-ai и frontend-зависимости; запустить команды README; выставить frontend live-режим; выполнить контрольный сценарий и один реальный AI-анализ с настроенной моделью. Любые изменения данных требуют повторной генерации fixtures и проверки контрольных чисел.
